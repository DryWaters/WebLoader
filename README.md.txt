Group project to show the time difference between concurrent and single threaded HTML page loading.

You can pass in an argument of a text file to load or if none are used "urls.txt" is default file that 
it looks for.

Written in Java